#include<stdio.h>
int main()
{
	int a[10],x=40,i,n;
	scanf("%d",&n);
	for(i=0;i<n;i++)
	{
		scanf("%d",&a[i]);
	}
	for(i=0;i<n;i++)
	{
		if(a[i]==x)
		{
			
		printf("element found at index",a[i]);
		break;
	}
	else
	{
		print("element not found");
		return 0;
	}
}
}
